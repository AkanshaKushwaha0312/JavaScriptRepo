var x = 1;
var y = 2;
(function(x, y) {
  x = x + y; //
  y = x - y;
  x = x - y;
})
(x, y);
console.log(x, y);

Output :: 1 2
Explaination :: Call by value.The call by value method of passing arguments to a function copies the actual value of an argument into the formal parameter of the function.
                In this case, changes made to the parameter inside the function have no effect on the argument. 
                